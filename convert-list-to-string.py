#Given a list of size n, convert it to a string.
#In python we can use the build in function join()

list1 = ['Hello', 'world', 'from', 'Python']

word = ' '.join(list1) #'' is used to specify the spaces between each element of the list 
print(word)